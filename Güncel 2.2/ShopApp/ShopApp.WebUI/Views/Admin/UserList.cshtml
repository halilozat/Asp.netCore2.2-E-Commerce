@model IEnumerable<ApplicationUser>

@{
    Layout = "_LayoutAdmin";
    ViewBag.Title = "All Users";
}


<h1 class="mt-5">Kullanıcılar</h1>
<hr />

<a class="btn btn-success  mb-3 btn-lg" href="/admin/createcategory">Kullanıcı Ekle</a>
<div class="card mb-4">
    <div class="card-header">
        <i class="fas fa-table mr-1"></i>
        Kullanıcılar
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-bordered" id="usersTable" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <td>Tam Adı</td>
                        <td>Kullanıcı Adı</td>
                        <td>Email</td>
                        <td>Telefon Numarası</td>
                        <td style="width: 160px;"></td>
                    </tr>
                </thead>

                <tbody>

                    @if (Model.Count() > 0)
                    {
                        @foreach (var item in Model)
                        {
                            <tr @*class="@(item.EmailConfirmed?"":"bg-warning")"*@>
                                <td>@item.FullName</td>
                                <td>@item.UserName</td>
                                <td>@item.Email</td>
                                <td>@item.PhoneNumber</td>
                                <td>
                                    <a href="/admin/user/@item.Id" class="btn btn-primary btn-sm mr-2">Düzenle</a>

                                    <form action="/admin/user/delete" method="POST" style="display: inline;">
                                        <input type="hidden" name="UserId" value="@item.Id">
                                        <button type="submit" class="btn btn-danger btn-sm">Sil</button>
                                    </form>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <div class="alert alert-warning">
                            <h3>Kayıtlı Kullanıcı Yok!</h3>
                        </div>
                    }

                </tbody>
            </table>
        </div>
    </div>
</div>


@section Scripts
{

    <script>
        $(document).ready(function () {
            $('#usersTable').DataTable();
        });
    </script>

}


















@*<div class="row">
    <div class="col-md-12">
        <h1 class="h3">User List</h1>
        <hr>
        <a class="btn btn-primary btn-sm" href="/admin/user/create">Create User</a>
        <table data-page-length='3' id="myTable" class="table table-bordered mt-3">
            <thead>
                <tr>
                    <td>Id</td>
                    <td>Tam Adı</td>
                    <td>Kullanıcı Adı</td>
                    <td>Email</td>
                    <td>Telefon Numarası</td>
                    <td style="width: 160px;"></td>
                </tr>
            </thead>
            <tbody>
                @if (Model.Count() > 0)
                {
                    @foreach (var item in Model)
                    {
                        <tr class="@(item.EmailConfirmed?"":"bg-warning")">
                            <td>@item.Id</td>
                            <td>@item.FullName</td>
                            <td>@item.UserName</td>
                            <td>@item.Email</td>
                            <td>@item.PhoneNumber</td>
                            <td>
                                <a href="/admin/user/@item.Id" class="btn btn-primary btn-sm mr-2">Düzenle</a>

                                <form action="/admin/user/delete" method="POST" style="display: inline;">
                                    <input type="hidden" name="UserId" value="@item.Id">
                                    <button type="submit" class="btn btn-danger btn-sm">Sil</button>
                                </form>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <div class="alert alert-warning">
                        <h3>Kayıtlı Kullanıcı Yok!</h3>
                    </div>
                }


            </tbody>
        </table>
    </div>
</div>*@
