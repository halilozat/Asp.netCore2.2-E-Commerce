@*@model CategoryListViewModel


<div class="list-group">

    @foreach (var item in Model.Categories)
    {
        <a asp-controller="Shop"
           asp-action="List"
           asp-route-category="@item.Name.ToLower()"
           class="list-group-item list-group-item-action @(Model.SelectedCategory==item.Name.ToLower()?"active":"")">
            @item.Name
        </a>
    }

</div>*@
@model CategoryListViewModel
<div class="list-group">
    <div>
        <div class="hero__categories">
            <div class="hero__categories__all">
                <i class="fa fa-bars"></i>
                <span>Kategori</span>
            </div>
            <ul>
                @foreach (var item in Model.Categories)
                {
                    <li><a asp-controller="Shop" asp-action="List" asp-route-category="@item.Name.ToLower()">@item.Name</a></li>
                }



                
            </ul>
        </div>
    </div>
    
</div>
